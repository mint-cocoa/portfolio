[project]
name = "portfolio-ops-api"
version = "0.1.0"
description = "Read-only operations API for portfolio homelab dashboard"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "httpx>=0.27.0",
  "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
